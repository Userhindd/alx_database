-- This script lists all databases in the MySQL server

-- Connect to the MySQL server with the provided credentials
-- -h: Host (localhost in this case)
-- -u: User (root in this case)
-- -p: Prompt for password
-- Query to list all databases
SHOW DATABASES;